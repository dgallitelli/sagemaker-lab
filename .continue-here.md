# Continue Here — 2026-03-15T18:03+08:00

## What Was Done This Session

### 1. Blog Post Polish (aws_blog_post.md)
- Restructured to AWS blog format: problem → architecture → deep dive → results → deployment → repo → conclusion
- Condensed "How SPLADE Works" from academic section to 1 paragraph overview, moved mechanics to Deep Dive
- Removed duplicate FLOPS regularization explanation
- Added "vs Zero-shot" AND "vs BM25" delta columns to all results tables
- Added "When NOT to Use SPLADE" section (multilingual, pure semantic, short texts)
- Added "Why No Dense Embedding Baseline?" section referencing BEIR/MTEB
- Removed fabricated BGE NDCG numbers that couldn't be verified
- Fixed training cost from wrong "$2-10" to correct "$6-17" (verified: $5.67/hr spot × 1-3hr)
- Removed CC BY-NC-SA 4.0 licensing paragraph (not needed for AWS blog)
- Added links throughout (SPLADE paper, sentence-transformers, HF model, BEIR, AWS docs)
- Rewrote conclusion to synthesize (3 takeaways) instead of repeating (was 4 redundant points)
- Replaced ASCII architecture diagram with Mermaid → rendered PNG at assets/architecture.png
- Critic agent (Opus) reviewed and prioritized P0/P1/P2 fixes; all structural fixes applied
- All numbers verified against training logs (agent did full audit)

### 2. NFCorpus Retrain Launched
- `splade-nfcorpus-20260315170905` on g5.2xlarge (fixed code with best-model selection)

### 3. Dense Baseline Built and Launched
- Created `src/dense_baseline.py` — BGE-large-en-v1.5 eval on all 3 datasets
- Created `src/launch_dense_baseline.py` — SageMaker launcher with multi-channel input
- Created `src/requirements_dense.txt`
- `dense-baseline-20260315175453` on g5.2xlarge — evaluates FiQA, NFCorpus, ESCI-200k sequentially

## Running Jobs

| Job | Instance | Purpose | Status at pause |
|-----|----------|---------|-----------------|
| `splade-esci-200k-20260315163644` | g5.12xlarge | ESCI v1 (pre-optimization code) | Training |
| `splade-esci-200k-v2-20260315170040` | g5.12xlarge | ESCI v2 (with optimizations) | Training |
| `splade-nfcorpus-20260315170905` | g5.2xlarge | NFCorpus retrain (fixed code) | Training |
| `dense-baseline-20260315175453` | g5.2xlarge | BGE-large-en-v1.5 on all 3 datasets | Downloading |

Expected completion order: NFCorpus → dense baseline → ESCI v1/v2

## What To Do Next

1. **Collect NFCorpus retrain results** — fill blog table (lines ~100-107)
2. **Collect dense baseline results** — download `dense_baseline_results.json` from S3
   ```
   aws s3 cp s3://sagemaker-us-east-1-859755744029/dense-baseline-output/<job>/output/model.tar.gz /tmp/ && tar xzf /tmp/model.tar.gz -C /tmp/dense_output/
   ```
3. **Collect ESCI-200k results** (both v1 and v2) — fill blog table, compare timing
4. **Add dense baseline to blog** — new column or separate comparison table with BGE-large numbers
5. **Fill GitHub repo URL** in blog (line ~270 "TODO: add repo URL")
6. **Test deployment** with FiQA model artifact (`deploy_endpoint.py`)
7. **Blog final polish** — remove all "pending" notes, preview, publish

## Files Changed (uncommitted)
- `aws_blog_post.md` — full blog restructure + polish (TRACKED, modified)
- `assets/architecture.png` — Mermaid-rendered pipeline diagram (NEW)
- `diagram.mmd` — Mermaid source (NEW, can delete)
- `src/dense_baseline.py` — BGE-large evaluation script (NEW)
- `src/launch_dense_baseline.py` — SageMaker launcher for dense baseline (NEW)
- `src/requirements_dense.txt` — deps for dense baseline (NEW)

## Key Decisions Made
- **g5.2xlarge for NFCorpus** — same GPU as FiQA, plenty for 3.6K corpus
- **BGE-large-en-v1.5** chosen as dense baseline (335M params, strong BEIR, open-weight)
- **Removed fabricated BEIR per-dataset numbers** — only cite verifiable aggregate data
- **No licensing section in blog** — not needed for AWS blog format
