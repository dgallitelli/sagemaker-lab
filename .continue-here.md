# Continue Here — 2026-03-15 22:30

## What Was Done This Session

### 1. NFCorpus Retrain Results Collected
- Job `splade-nfcorpus-20260315170905` completed (55 min)
- Best-model selection worked: ANCE regressed (0.3277), Phase 1 restored (0.3678)
- Blog updated with results + analysis

### 2. Dense Baseline (BGE-large-en-v1.5) — All 3 Datasets
- Job `dense-baseline-20260315203803` completed (52 min) after fixing:
  - `precision="float16"` invalid → use `model.half()` instead
  - Added BGE query prefix: `"Represent this sentence for searching relevant passages: "`
  - Reordered datasets smallest-first (nfcorpus → fiqa → esci)
  - Increased batch_size 128→256, timeout 2h→4h
- Results:
  - NFCorpus: NDCG=0.3820, Recall=0.3639, MRR=0.5759
  - FiQA: NDCG=0.4501, Recall=0.7697, MRR=0.5343
  - ESCI: NDCG=0.4412, Recall=0.6253, MRR=0.6543

### 3. Blog Post Updated with Dense Baseline
- All 3 tables now include BGE-large column with ◄ markers on best values
- "Why No Dense Baseline?" replaced with "SPLADE vs Dense Embeddings" analysis
- FiQA/NFCorpus analysis rewritten to acknowledge BGE strengths

### 4. Bedrock Baseline Script Created
- `src/bedrock_baseline.py` — evaluates Titan v2, Nova Multimodal, Cohere v4
- Concurrent ThreadPoolExecutor with retry/backoff for throttling
- Titan v2 NFCorpus result captured: NDCG=0.3400, Recall=0.2898, MRR=0.5484

## Bedrock Baseline Bugs to Fix (3 bugs in src/bedrock_baseline.py)

### Bug 1: Nova Multimodal API format
Model ID: `amazon.nova-2-multimodal-embeddings-v1:0`
Error: `required key [messages] not found`
The `_invoke_nova()` function uses `inputText` but Nova needs a different format.
Check AWS docs: https://docs.aws.amazon.com/nova/latest/nova2-userguide/embeddings.html

### Bug 2: Cohere v4 throttling
Even at 10 workers, Cohere hits rate limits on query encoding.
Fix: reduce to 3-5 workers, increase backoff, or add sleep between batches.

### Bug 3: Titan v2 empty string
FiQA corpus has empty doc texts after `build_doc_text()`.
Error: `expected minLength: 1, actual: 0`
Fix: in `build_doc_text()` or `encode_bedrock()`, replace empty strings with "(empty)".

## Still Running SageMaker Jobs (check first!)
```bash
aws sagemaker describe-training-job --training-job-name splade-esci-200k-20260315163644 --query '{Status: TrainingJobStatus}' --output text
aws sagemaker describe-training-job --training-job-name splade-esci-200k-v2-20260315170040 --query '{Status: TrainingJobStatus}' --output text
```
- ESCI v1: was in ANCE mining (~305 min in, pre-optimization code)
- ESCI v2: Phase 1 done (NDCG=0.4901, Recall=0.6859, MRR=0.6884), was in ANCE phase
- To get results: check CloudWatch logs (log stream names in MEMORY.md)

## Remaining Work
1. Fix 3 Bedrock bugs, rerun on NFCorpus + FiQA (skip ESCI — too slow via API)
2. Collect ESCI SPLADE results and fill blog table (lines ~67-78)
3. Add Bedrock + Titan comparison paragraph to blog
4. Add GitHub repo URL to blog (line ~270)
5. Final polish: remove "TBD"/"pending", preview, publish

## Key Files
- `aws_blog_post.md` — blog (ESCI SPLADE TBD, Bedrock not yet added)
- `src/bedrock_baseline.py` — needs 3 bug fixes
- `src/dense_baseline.py` — working, completed
- `bedrock_baseline_results.json` — partial (Titan v2 NFCorpus only)

## Uncommitted Changes
```
 M aws_blog_post.md
 M src/dense_baseline.py
 M src/launch_dense_baseline.py
 M src/requirements_dense.txt
?? bedrock_baseline_results.json
?? src/bedrock_baseline.py
```
